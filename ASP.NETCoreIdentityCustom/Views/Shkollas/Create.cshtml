@model ASP.NETCoreIdentityCustom.Models.Shkolla

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Shkolla</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EmriShkolles" class="control-label"></label>
                <input asp-for="EmriShkolles" class="form-control" />
                <span asp-validation-for="EmriShkolles" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SelectedLlojiShkolles" class="control-label"></label>
                <select asp-for="SelectedLlojiShkolles" asp-items="@(new SelectList(Model.LlojiShkolles))"></select>
                <span asp-validation-for="SelectedLlojiShkolles" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
